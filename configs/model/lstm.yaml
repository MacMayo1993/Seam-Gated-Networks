# Standard LSTM Configuration

_target_: lightning_module.SeamGatedLightningModule

model_type: lstm
input_dim: 4
hidden_dim: 64
learning_rate: 1e-3
weight_decay: 0.0
max_epochs: 50
warmup_epochs: 5
gradient_clip_val: 1.0
use_amp: true

# LSTM specific
num_layers: 1
dropout: 0.0
